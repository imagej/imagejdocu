====== Cut ======

**Cut command**

The Cut command copies the contents of the current image **selection** (or the whole image if no selection is active) to the internal clipboard and fills it with the current background color.
